@{
    ViewBag.Title = "Test";
}

@foreach (var question in Model)
{
    <p>@question.QuestionText</p>
    foreach (var answer in question.Answers)
    {
        @InputExtensions.RadioButton(Html, question.QuestionText, 1);
        <a>@answer.Value</a>
        <br />
    }
}